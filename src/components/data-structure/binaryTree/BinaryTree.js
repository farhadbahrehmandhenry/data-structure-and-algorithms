import React from 'react';

const BinaryTree = () => {
  return (
    <div>
      <li>in singly linked list each node was linked to only one node, nut in tree each node can link to 0 to 2 nodes</li>
      <li>tree has a parent-children relationship between nodes</li>
      <li>it's not sorted</li>
      <li>top node is ROOT</li>
      <li>children of one node are SIBLINGS</li>
      <li>the node that has no child is LEAF</li>
      <li>examples of trees: html DOM - file directory</li>
    </div>
  );
}

export default BinaryTree;